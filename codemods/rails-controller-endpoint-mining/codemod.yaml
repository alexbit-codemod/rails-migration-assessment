schema_version: "1.0"

name: "rails-controller-endpoint-mining"
version: '0.1.2'
description: "Mine Rails controller actions as API endpoints for Rails migration assessment"
author: "Alex Bit <alex@codemod.com>"
license: "MIT"


targets:
  # Keep this aligned with the files matched in workflow.yaml.
  languages: ["ruby"]

keywords: ["mining", "metrics", "rails", "ruby", "migration-assessment"]

registry:
  access: 'pro'
  visibility: 'public'


capabilities: []

workflows:
  - name: main
    default: true
    path: workflow.yaml
